// Code generated by surface-gen; DO NOT EDIT.
import { getSmsTemplate, listSmsTemplates } from "../generated/sdk.gen.js";
import type { GetSmsTemplateData, ListSmsTemplatesData, SmsTemplate, SmsTemplateSummary } from "../generated/types.gen.js";
import { Resource } from "./base.js";
import type { APIPromise, PaginatedPromise, RequestOptions } from "../core/result.js";

export type { SmsTemplateSummary };
export type { SmsTemplate };
export type SmsTemplateListQuery = NonNullable<ListSmsTemplatesData["query"]>;

export class SmsTemplatesResourceBase extends Resource {
  /**
   * List workspace and built-in SMS templates as a cursor page. Filter by scope, category, status, language, or a search across slug, name, and description. Read a version to retrieve content and variables.
   *
   * @example List the built-in templates
   * for await (const tpl of bird.smsTemplates.list({ scope: "system" })) {
   *   console.log(tpl.id, tpl.slug);
   * }
   */
  list(query?: SmsTemplateListQuery, options?: RequestOptions): PaginatedPromise<SmsTemplateSummary> {
    return this.paginated<SmsTemplateSummary>("GET", options, ({ signal, headers }, cursor) =>
      listSmsTemplates({ client: this.client, query: { ...query, starting_after: cursor ?? query?.starting_after, ending_before: cursor === undefined ? query?.ending_before : undefined }, headers, signal }));
  }

  /**
   * Read one SMS template's metadata, language states, draft revision, and draft and live version IDs. The response omits content and variables; read a version to retrieve them.
   *
   * @example Read a template's live version
   * const tpl = await bird.smsTemplates.get("bird_otp_verification");
   * console.log(tpl.default_language, tpl.live_version_id);
   */
  get(templateRef: string, options?: RequestOptions): APIPromise<SmsTemplate> {
    return this.call<SmsTemplate>("GET", options, ({ signal, headers }) =>
      getSmsTemplate({ client: this.client, path: { template_ref: templateRef }, headers, signal }));
  }
}
