// Code generated by surface-gen; DO NOT EDIT.
import { createVoiceTrunkGateway, deleteVoiceTrunkGateway, getVoiceTrunkGateway, listVoiceTrunkGateways, updateVoiceTrunkGateway } from "../generated/sdk.gen.js";
import type { CreateVoiceTrunkGatewayData, DeleteVoiceTrunkGatewayData, GetVoiceTrunkGatewayData, ListVoiceTrunkGatewaysData, UpdateVoiceTrunkGatewayData, VoiceTrunkGateway, VoiceTrunkGatewayList } from "../generated/types.gen.js";
import { Resource } from "./base.js";
import type { APIPromise, RequestOptions } from "../core/result.js";

export type { VoiceTrunkGatewayList };
export type { VoiceTrunkGateway };
export type VoiceTrunksGatewaysCreateParams = NonNullable<CreateVoiceTrunkGatewayData["body"]>;
export type VoiceTrunksGatewaysUpdateParams = NonNullable<UpdateVoiceTrunkGatewayData["body"]>;

export class VoiceTrunksGatewaysResource extends Resource {
  /**
   * Read the complete inbound gateway list for a trunk, ordered by priority. Available even when inbound calling is disabled.
   *
   * @example List a trunk's gateways
   * const gateways = await bird.voice.trunks.gateways.list("TRUNK_ID");
   * for (const gateway of gateways.data) {
   *   console.log(gateway.id, gateway.priority);
   * }
   */
  list(trunkId: string, options?: RequestOptions): APIPromise<VoiceTrunkGatewayList> {
    return this.call<VoiceTrunkGatewayList>("GET", options, ({ signal, headers }) =>
      listVoiceTrunkGateways({ client: this.client, path: { trunk_id: trunkId }, headers, signal }));
  }

  /**
   * Read one inbound gateway and its SIP address, priority, and number formats. The gateway must belong to the specified trunk in this workspace.
   *
   * @example Read one gateway
   * const gateway = await bird.voice.trunks.gateways.get("TRUNK_ID", "GATEWAY_ID");
   * console.log(gateway.id, gateway.priority);
   */
  get(trunkId: string, gatewayId: string, options?: RequestOptions): APIPromise<VoiceTrunkGateway> {
    return this.call<VoiceTrunkGateway>("GET", options, ({ signal, headers }) =>
      getVoiceTrunkGateway({ client: this.client, path: { trunk_id: trunkId, gateway_id: gatewayId }, headers, signal }));
  }

  /**
   * Add an address for inbound call delivery. Enable inbound calling on the trunk first; a trunk supports up to 20 gateways.
   *
   * @example Add a gateway to a trunk
   * const gateway = await bird.voice.trunks.gateways.create("TRUNK_ID", {
   *   sip_uri: "sip:pbx.example.com:5060",
   *   priority: 0,
   *   destination_format: "1234#{number}",
   * });
   * console.log(gateway.id, gateway.priority);
   */
  create(trunkId: string, params: VoiceTrunksGatewaysCreateParams, options?: RequestOptions): APIPromise<VoiceTrunkGateway> {
    return this.call<VoiceTrunkGateway>("POST", options, ({ signal, headers }) =>
      createVoiceTrunkGateway({ client: this.client, path: { trunk_id: trunkId }, body: params, headers, signal }));
  }

  /**
   * Change an inbound gateway address, priority, or number formats. Omitted fields stay unchanged; an empty format restores E.164. Requires inbound calling enabled.
   *
   * @example Change a gateway's priority
   * const gateway = await bird.voice.trunks.gateways.update("TRUNK_ID", "GATEWAY_ID", {
   *   priority: 10,
   * });
   * console.log(gateway.id, gateway.priority);
   */
  update(trunkId: string, gatewayId: string, params: VoiceTrunksGatewaysUpdateParams = {}, options?: RequestOptions): APIPromise<VoiceTrunkGateway> {
    return this.call<VoiceTrunkGateway>("PATCH", options, ({ signal, headers }) =>
      updateVoiceTrunkGateway({ client: this.client, path: { trunk_id: trunkId, gateway_id: gatewayId }, body: params, headers, signal }));
  }

  /**
   * Remove an inbound gateway. Removing the last gateway stops new inbound delivery through the trunk. Requires inbound calling enabled.
   *
   * @example Remove a gateway
   * await bird.voice.trunks.gateways.delete("TRUNK_ID", "GATEWAY_ID");
   */
  delete(trunkId: string, gatewayId: string, options?: RequestOptions): APIPromise<void> {
    return this.call<void>("DELETE", options, ({ signal, headers }) =>
      deleteVoiceTrunkGateway({ client: this.client, path: { trunk_id: trunkId, gateway_id: gatewayId }, headers, signal }));
  }
}
