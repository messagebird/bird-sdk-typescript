// Code generated by surface-gen; DO NOT EDIT.
import { rotateWhatsAppGroupInviteLink } from "../generated/sdk.gen.js";
import type { RotateWhatsAppGroupInviteLinkData, WhatsAppGroupInviteLink } from "../generated/types.gen.js";
import { Resource } from "./base.js";
import type { APIPromise, RequestOptions } from "../core/result.js";

export type { WhatsAppGroupInviteLink };

export class WhatsappGroupsInviteLinkResource extends Resource {
  /**
   * Issue a new invite link for the group and invalidate every link issued before. Anyone holding an old link can no longer join. The group's current link is on the group itself, so read it rather than rotating to fetch it. Answers 200: the new link comes back on the call itself.
   *
   * @example Issue a new invite link
   * const link = await bird.whatsapp.groups.inviteLink.rotate("wag_01krdgeqcxet5s7t44vh8rt9mg");
   * console.log(link.invite_link); // every earlier link has stopped working
   */
  rotate(groupId: string, options?: RequestOptions): APIPromise<WhatsAppGroupInviteLink> {
    return this.call<WhatsAppGroupInviteLink>("POST", options, ({ signal, headers }) =>
      rotateWhatsAppGroupInviteLink({ client: this.client, path: { group_id: groupId }, headers, signal }));
  }
}
