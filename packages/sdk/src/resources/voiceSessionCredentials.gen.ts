// Code generated by surface-gen; DO NOT EDIT.
import { createVoiceSessionCredential } from "../generated/sdk.gen.js";
import type { CreateVoiceSessionCredentialData, VoiceSessionCredential } from "../generated/types.gen.js";
import { Resource } from "./base.js";
import type { APIPromise, RequestOptions } from "../core/result.js";

export type { VoiceSessionCredential };

export class VoiceSessionCredentialsResource extends Resource {
  /**
   * Mint a short-lived SIP digest credential a calling client authenticates with. The password is returned once and cannot be retrieved again.
   *
   * @example Mint a SIP credential
   * const credential = await bird.voice.sessionCredentials.create();
   * // The password is returned once. Until `expires_at` it can place billed calls.
   * console.log(credential.username, credential.realm, credential.expires_at);
   */
  create(options?: RequestOptions): APIPromise<VoiceSessionCredential> {
    return this.call<VoiceSessionCredential>("POST", options, ({ signal, headers }) =>
      createVoiceSessionCredential({ client: this.client, headers, signal }));
  }
}
